{
    "name": "@mzero-labs/spog",
    "version": "1.0.0",
    "description": "SPOG contract ABIs",
    "main": "index.js",
    "author": "mZero Labs <contact@m0.xyz>",
    "license": "GPL-3.0",
    "scripts": {
        "prettier": "prettier --write 'script/**/*.sol' 'src/**/*.sol' 'test/**/*.sol'",
        "solhint": "solhint -f stylish 'src/**/*.sol'",
        "solhint-fix": "solhint --fix 'src/**/*.sol'",
        "typechain-ethers": "typechain --target ethers-v5 --out-dir types/ethers --always-generate-overloads 'out/*.sol/*.json'"
    },
    "devDependencies": {
        "@typechain/ethers-v5": "^11.1.1",
        "prettier": "^3.0.0",
        "prettier-plugin-solidity": "^1.1.3",
        "solhint": "^3.4.1",
        "typechain": "^8.3.1",
        "typescript": "^5.2.2"
    },
    "engines": {
        "node": ">=18"
    }
}
